const employee = require('../lib/engineer');

describe("employee", () => {
 
    it("should return employee information", () => {
      const str = new employee("bob","79","bob@bob.com",)
    });
  });